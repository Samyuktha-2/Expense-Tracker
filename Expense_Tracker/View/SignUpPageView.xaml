<UserControl x:Class="Expense_Tracker.View.SignUpPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Expense_Tracker.View"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Background="#F6F7FB">

        <Grid.Resources>

            <!-- Animated TextBox -->
            <Style x:Key="AnimatedTextBox"
                   TargetType="TextBox">
                <Setter Property="Height"
                        Value="40" />
                <Setter Property="Padding"
                        Value="5" />
                <Setter Property="FontSize"
                        Value="14" />
                <Setter Property="BorderThickness"
                        Value="1" />
                <Setter Property="BorderBrush"
                        Value="#D0D5DD" />
                <Setter Property="Background"
                        Value="#FAFAFA" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">

                            <Border x:Name="border"
                                    CornerRadius="6"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}">

                                <ScrollViewer x:Name="PART_ContentHost" />

                            </Border>

                            <ControlTemplate.Triggers>

                                <Trigger Property="IsFocused"
                                         Value="True">
                                    <Setter TargetName="border"
                                            Property="BorderBrush"
                                            Value="#6366F1" />
                                    <Setter TargetName="border"
                                            Property="BorderThickness"
                                            Value="2" />
                                </Trigger>

                            </ControlTemplate.Triggers>

                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

            </Style>

            <!-- Rounded Button -->
            <Style x:Key="RoundedButton"
                   TargetType="Button">

                <Setter Property="Height"
                        Value="40" />
                <Setter Property="Foreground"
                        Value="White" />
                <Setter Property="Background"
                        Value="#6366F1" />
                <Setter Property="FontWeight"
                        Value="SemiBold" />
                <Setter Property="BorderThickness"
                        Value="0" />

                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">

                            <Border x:Name="btnBorder"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">

                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />

                            </Border>

                            <ControlTemplate.Triggers>

                                <Trigger Property="IsMouseOver"
                                         Value="True">
                                    <Setter TargetName="btnBorder"
                                            Property="Background"
                                            Value="#4F46E5" />
                                </Trigger>

                                <Trigger Property="IsPressed"
                                         Value="True">
                                    <Setter TargetName="btnBorder"
                                            Property="Opacity"
                                            Value="0.85" />
                                </Trigger>

                            </ControlTemplate.Triggers>

                        </ControlTemplate>
                    </Setter.Value>
                </Setter>

            </Style>

        </Grid.Resources>

        <Border CornerRadius="15"
                Background="White"
                Width="420"
                Height="480"
                Padding="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <Border.Effect>
                <DropShadowEffect BlurRadius="10"
                                  Opacity="0.25" />
            </Border.Effect>

            <StackPanel x:Name="SignUpPanel">

                <!-- Title -->
                <TextBlock Text="Create Account"
                           FontSize="26"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 25" />

                <!-- Email -->
                <TextBlock Text="Email" />
                <TextBox Style="{StaticResource AnimatedTextBox}"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 5 0 15" />

                <!-- Name -->
                <TextBlock Text="Name" />
                <TextBox Style="{StaticResource AnimatedTextBox}"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0 5 0 15" />

                <!-- Password -->
                <TextBlock Text="Password" />
                <PasswordBox Margin="0 5 0 15" 
                             Height="38"
                             Padding="10"
                             Background="#FAFAFA"
                             x:Name="PasswordBox"
                             BorderBrush="#D0D5DD"
                             BorderThickness="1" />

                <!-- Confirm Password -->
                <TextBlock Text="Confirm Password" />
                <PasswordBox Margin="0 5 0 20"
                             Height="38"
                             Padding="10"
                             x:Name="ConfirmPasswordBox"
                             Background="#FAFAFA"
                             BorderBrush="#D0D5DD"
                             BorderThickness="1" />
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="Red"
                           Margin="50 0 50 5"
                           FontSize="12" />

                <!-- Signup Button -->
                <Button Content="Sign Up"
                        Style="{StaticResource RoundedButton}"
                        Command="{Binding SignupCommand}" 
                        CommandParameter="{Binding ElementName=SignUpPanel}"/>

            </StackPanel>

        </Border>

    </Grid>

</UserControl>
